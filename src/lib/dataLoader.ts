import { Winery } from "@/types/minting";

export async function loadWineriesData(): Promise<Winery[]> {
  // This function is kept for future use when the API is available
  // For now, data is loaded directly in the component
  throw new Error("API not yet implemented - using component-level data loading");
}
